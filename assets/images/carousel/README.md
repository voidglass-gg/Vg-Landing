# Images du carrousel

Déposez les nouvelles images dans ce dossier, puis ajoutez une balise `img` dans
le bloc `.hero-carousel` de `index.html`. Le script les inclut automatiquement
dans la rotation, dans l'ordre du HTML.

Conservez idéalement un ratio de 16:10 pour éviter tout recadrage inattendu.
